for some reason cursor's implementation of .md syntax highlighting only shows a subset of the supposed syntax coloring.
let's build a extension to override it.

after u think it works, let's make public repo under this and have cicd to auto build vsix